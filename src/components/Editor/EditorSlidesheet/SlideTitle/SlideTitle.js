import React from 'react';

const SlideTitle = (props) => {
        return (
            <div className="SlideTitle">
            <h1>{props.title}</h1>
            </div>
        );
}

export default SlideTitle;